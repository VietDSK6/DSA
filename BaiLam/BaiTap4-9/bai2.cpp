#include <bits/stdc++.h>

using namespace std;

void solve(string a, string b) {
    for (int i = 0; i < a.length(); i++) {
        
    }
}

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(nullptr);

  int tc = 1;
  cin >> tc;
  string a, b;

  while (tc--) {
    cin >> a >> b;
    solve(a, b);
  }
}
